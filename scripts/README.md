# Scripts

## new-app.mjs

This script generates a new app from the template.

### Example

```bash
npm run new:app -- \
  --dest ../photo-to-pdf-app \
  --displayName "Photo to PDF" \
  --jsName PhotoToPDF \
  --bundleId com.cleanutilityapps.phototopdf \
  --version 1.0.0 \
  --build 1
```
